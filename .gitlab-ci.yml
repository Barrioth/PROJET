stages:
  - test
  - deploy

tests:
  stage: test
  tags:
    - dockerbr
  script:
    - echo "Running tests"
    - python --version
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"

deploy_production:
  stage: deploy
  tags:
    - dockerbr
  script:
    - wsl bash -c "sudo ansible-playbook -i /mnt/c/GitLab-Runner/builds/Rfzfr-AOv/0/Barrioth/PROJET/terraform/inventory.ini /mnt/c/GitLab-Runner/builds/Rfzfr-AOv/0/Barrioth/PROJET/playbook.yml"
  rules:
    - if: $CI_COMMIT_BRANCH == "9"