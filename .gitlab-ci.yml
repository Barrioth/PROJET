stages:
  - test
  - deploy

tests:
  stage: test
  tags:
    - dockerbr
  script:
    - echo "Running tests"
    - python --version
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"

deploy_production:
  stage: deploy
  tags:
    - dockerbr
  script:
    - wsl mkdir -p ~/.ssh
    - wsl bash -c "cat /home/lagia/.ssh/id_ed25519_pasta > /home/qurupeco/.ssh/id_ed25519_pasta && chmod 600 /home/qurupeco/.ssh/id_ed25519_pasta"
    - wsl bash -c "cd /mnt/c/GitLab-Runner/builds/Rfzfr-AOv/0/Barrioth/PROJET && ansible-playbook -i terraform/inventory.ini playbook.yml"
  rules:
    - if: $CI_COMMIT_BRANCH == "9"